# Welcome to your prefect.yaml file! You can use this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: Stock_Project
prefect-version: 3.4.18

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations
pull:
- prefect.deployments.steps.set_working_directory:
    directory: /home/elias/Projects/Stock_Project

# the deployments section allows you to provide configuration for deploying flows
deployments:
- name: Deploy
  version: null
  tags: []
  concurrency_limit: null
  description: |-
    Orchestrate daily ingestion for inference.

    Parameters
    - tickers: Optional override for tickers dict.
    - end_date: Optional ISO date/time string; defaults to now if not provided.
    - up_to_days: Lookback window in days for initial data fetch.
    - output_path: Where to save the aggregated dataset.
  entrypoint: workflows/daily_ingestion_flow.py:daily_ingestion_flow
  parameters: {}
  work_pool:
    name: runner
    work_queue_name: null
    job_variables: {}
  schedules:
  - cron: 30 2 * * 1-5
    timezone: Africa/Lagos
    day_or: true
    active: false
